<h1><%= @artist.name %></h1>

<ul>
  <% @artist.artworks.each do |artwork| %>
    <li><%= link_to artwork.name, artwork_path(artwork) %></li>
  <% end %>
</ul>

<%= link_to 'Back', artists_path %>
<br>
<br>
