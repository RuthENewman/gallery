<h1>Artists in the gallery</h1>

<table>
  <thead>
    <tr>
      <th>Artist name</th>
      <th colspan="3"></th>
      </tr>
  </thead>

  <tbody>
  <% @artists.each do |artist| %>
    <tr>
      <td><%= artist.name %></td>
      <td><%= link_to 'Profile page', artist %></td>
    <td><%= link_to 'Delete artist', artist, method: :delete, data: { confirm: 'Are you sure?'} %></td>
    </tr>
  <% end %>
  </tbody>
  </table>


<br>

<%= link_to 'Add an Artist', new_artist_path %>
<br>
<%= link_to 'Add an Artwork', new_artwork_path %>
